package com.thedotbot.touch;

import java.util.List;

public class SendEvents {

	public static void sendEvents(List<TouchEvent> events) {
		// popen a binary which reads scripts from stdin
		// for (TouchEvent event : events) {
		// 	  stdin.write(event.toString());
		// }
	}
	
}
